
class ShhUtils:

    def __init__(self):
        pass

    # def getShhServerConnection(self, host, username, password, ssh_pkey_path, ssh_pkey_password, remote_bind_address, remote_bind_port):
    #     server = SSHTunnelForwarder(
    #         (host, 22),
    #         ssh_username=username,
    #         ssh_password=password,
    #         ssh_pkey=ssh_pkey_path,
    #         ssh_private_key_password=ssh_pkey_password,
    #         remote_bind_address=(remote_bind_address, remote_bind_port)
    #     )
    #     return server